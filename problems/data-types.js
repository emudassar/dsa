// Declare an array scores with values [85, 90, 95]. Print the second element and the array length.
// Example Output: 90 3


let scores = [85, 90, 95]

console.log(scores[1], scores.length);


// Declare an object student with properties name (set to "Bob") and grade (set to 90). Print both properties.
// Example Output: "Bob 90"

let student = {
    name: "Bob",
    grade: 90
}

console.log(student.name, student.grade);
